#pragma once
#include <cstdint>
#include <string>

// Plotting directory - where finalized .gcode files are stored
inline const std::string PLOTTING_DIRECTORY = "plotting/";

// Temporary directory - for in-progress uploads and temporary files
inline const std::string TEMP_DIRECTORY = "tmp/";